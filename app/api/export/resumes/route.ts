import { requireUser } from "@/lib/api";
export async function GET() { const auth = await requireUser(); if (auth.error) return auth.error; const { data, error } = await auth.supabase.from("resumes").select("*").order("updated_at", { ascending: false }); if (error) return Response.json({ error: error.message }, { status: 500 }); const body = (data ?? []).map((resume) => `# ${resume.label}\n\n${resume.body}`).join("\n\n---\n\n"); return new Response(body, { headers: { "Content-Type": "text/markdown; charset=utf-8", "Content-Disposition": "attachment; filename=careeros-resumes.md" } }); }
