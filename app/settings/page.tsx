import { SetupNeeded } from "@/components/setup-needed"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { isSupabaseConfigured } from "@/lib/supabase/is-configured"; import { createClient } from "@/lib/supabase/server";
export default async function SettingsPage() { if (!isSupabaseConfigured()) return <SetupNeeded/>; const supabase = await createClient(); const [playbooks, calibrations, sessions] = await Promise.all([supabase.from("playbooks").select("slug, title, updated_at").order("title"), supabase.from("calibrations").select("*").order("created_at", { ascending: false }), supabase.from("sessions").select("type, model_costs, created_at").order("created_at", { ascending: false }).limit(30)]); return <div className="flex flex-col gap-4"><h1 className="text-lg font-semibold">Settings</h1><Card><CardHeader><CardTitle className="text-sm">Playbooks</CardTitle></CardHeader><CardContent><p className="mb-2 text-sm text-muted-foreground">Import repo playbooks from the admin route after adding the vault files.</p>{(playbooks.data ?? []).map((playbook) => <p className="text-sm" key={playbook.slug}>{playbook.title} <span className="text-muted-foreground">({playbook.slug})</span></p>)}</CardContent></Card><Card><CardHeader><CardTitle className="text-sm">Calibrations</CardTitle></CardHeader><CardContent>{(calibrations.data ?? []).length ? calibrations.data?.map((item) => <p className="text-sm" key={item.id}>{item.label}: comp floor {item.total_comp_floor ?? "-"}, brand {item.brand_weight}, exit {item.exit_weight}</p>) : <p className="text-sm text-muted-foreground">Create a calibration in Supabase before the first scan.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="text-sm">Recent spend sessions</CardTitle></CardHeader><CardContent>{(sessions.data ?? []).map((session) => <p className="text-sm" key={`${session.type}-${session.created_at}`}>{session.type} · {new Date(session.created_at).toLocaleString()}</p>)}</CardContent></Card></div>; }
