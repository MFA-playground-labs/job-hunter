import Link from "next/link"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { SetupNeeded } from "@/components/setup-needed"; import { isSupabaseConfigured } from "@/lib/supabase/is-configured"; import { createClient } from "@/lib/supabase/server"; import { saveMasterResume } from "./actions";
export default async function ResumesPage() { if (!isSupabaseConfigured()) return <SetupNeeded/>; const supabase = await createClient(); const { data: master } = await supabase.from("resumes").select("*").eq("master", true).maybeSingle(); return <div className="flex max-w-3xl flex-col gap-4"><div className="flex items-center justify-between"><h1 className="text-lg font-semibold">Resumes</h1><Link href="/api/export/resumes" className="text-sm underline">Export Markdown</Link></div><form action={saveMasterResume} className="flex flex-col gap-3"><Input name="label" defaultValue={master?.label ?? "Master resume"}/><Textarea name="body" className="min-h-96 font-mono text-xs" defaultValue={master?.body ?? ""} placeholder="Paste your master resume here"/><Button>Save master resume</Button></form></div>; }
