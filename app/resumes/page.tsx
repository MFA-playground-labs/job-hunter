import Link from "next/link"; import { ActionSubmit } from "@/components/action-submit"; import { PageHeader } from "@/components/page-header"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { SetupNeeded } from "@/components/setup-needed"; import { ErrorState } from "@/components/empty-state"; import { isSupabaseConfigured } from "@/lib/supabase/is-configured"; import { createClient } from "@/lib/supabase/server"; import { saveMasterResume } from "./actions";
export default async function ResumesPage(){if(!isSupabaseConfigured())return <SetupNeeded/>;const supabase=await createClient();const{data:master,error}=await supabase.from("resumes").select("*").eq("master",true).maybeSingle();if(error)return <ErrorState message={error.message}/>;return <div className="space-y-7"><PageHeader eyebrow="Career materials" title="Resumes" description="Keep one truthful master resume as the source for future tailoring. Adding it is optional while you explore the UI." actions={master?<Button render={<Link href="/api/export/resumes"/>} variant="outline">Export Markdown</Button>:undefined}/><section className="rounded-2xl border bg-background p-5 sm:p-6"><div className="mb-5"><h2 className="text-lg font-semibold">{master?"Master resume":"Add your master resume when ready"}</h2><p className="mt-1 text-sm text-muted-foreground">Paste Markdown or plain text. Nothing here is generated or submitted automatically.</p></div><form action={saveMasterResume} className="space-y-4"><label className="grid gap-2 text-sm font-medium">Label<Input className="h-11" name="label" defaultValue={master?.label??"Master resume"}/></label><label className="grid gap-2 text-sm font-medium">Resume content<Textarea name="body" className="min-h-[28rem] font-mono text-xs leading-5" defaultValue={master?.body??""} placeholder="Paste your master resume here" required/></label><ActionSubmit>Save master resume</ActionSubmit></form></section></div>}
