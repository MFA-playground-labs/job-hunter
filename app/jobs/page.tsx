import Link from "next/link"; import { Badge } from "@/components/ui/badge"; import { Button } from "@/components/ui/button"; import { SetupNeeded } from "@/components/setup-needed"; import { EmptyState } from "@/components/empty-state"; import { isSupabaseConfigured } from "@/lib/supabase/is-configured"; import { createClient } from "@/lib/supabase/server"; import { isStaleScan } from "@/lib/staleness"; import { updateJob } from "./actions";
export default async function JobsPage() { if (!isSupabaseConfigured()) return <SetupNeeded/>; const supabase = await createClient(); const { data } = await supabase.from("jobs").select("*, companies(name), job_scores(*)").order("scanned_at", { ascending: false }); const fresh = (data ?? []).filter((job) => job.status !== "passed" && !isStaleScan(job.scanned_at)); return <div className="flex flex-col gap-4"><h1 className="text-lg font-semibold">Jobs</h1>{!fresh.length ? <EmptyState title="No fresh jobs yet" description="Import playbooks, create a calibration, then scan your target companies."/> : fresh.map((job) => { const score = job.job_scores.sort((a, b) => b.created_at.localeCompare(a.created_at))[0]; return <article className="rounded-lg border p-4" key={job.id}><div className="flex flex-wrap items-start justify-between gap-3"><div><Link className="font-medium underline" href={`/jobs/${job.id}`}>{job.title}</Link><p className="text-sm text-muted-foreground">{job.companies?.name ?? "Unknown company"} · {job.location ?? "Location unknown"}</p></div>{score && <Badge>{score.composite.toFixed(0)} / 100</Badge>}</div>{score && <details className="mt-2 text-sm"><summary>Scoring rationale</summary><p className="mt-2">{score.rationale}</p></details>}<div className="mt-3 flex gap-2"><form action={updateJob}><input name="id" type="hidden" value={job.id}/><input name="status" type="hidden" value="interested"/><Button size="sm">Interested</Button></form><form action={updateJob} className="flex gap-1"><input name="id" type="hidden" value={job.id}/><input name="status" type="hidden" value="passed"/><select name="pass_reason" className="h-7 rounded border text-xs"><option value="other">Pass reason</option><option value="comp_too_low">Comp too low</option><option value="brand_mismatch">Brand mismatch</option><option value="wrong_scope">Wrong scope</option><option value="location">Location</option><option value="domain">Domain</option></select><Button size="sm" variant="outline">Pass</Button></form></div></article>; })}</div>; }
